/**
 * Punto de entrada estable (`SearchingProvider`): búsqueda de proveedor + spinners de carga.
 * Implementación única en SearchingProviderScreen.js — no duplicar lógica en este archivo.
 */
export { default } from './SearchingProviderScreen';
