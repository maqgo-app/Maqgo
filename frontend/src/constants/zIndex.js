/**
 * Escala de z-index - alineada con tokens.css
 * Usar en componentes con position fixed/absolute/sticky
 */
export const Z_INDEX = {
  base: 1,
  sticky: 10,
  dropdown: 100,
  floatingButton: 900,
  overlay: 1000,
  fixedBar: 9999,
  modal: 10000
};
