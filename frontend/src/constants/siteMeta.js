/**
 * Título y marca únicos para documento + SEO (debe coincidir con index.html).
 * Un solo string de producto; evita drift entre boot HTML y SPA.
 */
export const SITE_TITLE =
  'MAQGO | Maquinaria pesada donde la necesites con operador incluido';

export const SITE_TITLE_SHORT = 'MAQGO';
